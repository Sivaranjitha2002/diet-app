var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _NoSQL_requester;
import { Handler } from '@zcatalyst/transport';
import { CONSTANTS, isValidInputString, wrapValidators, wrapValidatorsWithPromise } from '@zcatalyst/utils';
import NoSQLTable from './table';
import { NoSQLByte } from './utils/byte';
import * as NoSQLEnum from './utils/enum';
import { CatalystNoSQLError } from './utils/error';
import { NoSQLItem } from './utils/item';
import { NoSQLMarshall } from './utils/marshall';
import { NoSQLByteSet, NoSQLNumberSet, NoSQLStringSet } from './utils/set';
import { NoSQLUnMarshall } from './utils/unmarshall';
const { COMPONENT, CREDENTIAL_USER, REQ_METHOD } = CONSTANTS;
export class NoSQL {
    constructor(app) {
        _NoSQL_requester.set(this, void 0);
        __classPrivateFieldSet(this, _NoSQL_requester, new Handler(app, this), "f");
    }
    getComponentName() {
        return COMPONENT.no_sql;
    }
    async getTable(tableId) {
        await wrapValidatorsWithPromise(() => {
            isValidInputString(tableId, 'table_id', true);
        }, CatalystNoSQLError);
        const request = {
            method: REQ_METHOD.get,
            path: `/nosqltable/${tableId}`,
            service: "baas",
            track: true,
            user: CREDENTIAL_USER.admin
        };
        const res = await __classPrivateFieldGet(this, _NoSQL_requester, "f").send(request);
        const tableData = res.data.data;
        return new NoSQLTable(__classPrivateFieldGet(this, _NoSQL_requester, "f"), { tableDetails: tableData });
    }
    async getAllTable() {
        const request = {
            method: REQ_METHOD.get,
            path: `/nosqltable`,
            service: "baas",
            track: true,
            user: CREDENTIAL_USER.admin
        };
        const res = await __classPrivateFieldGet(this, _NoSQL_requester, "f").send(request);
        const allTablesData = res.data.data;
        return allTablesData.map((tableData) => new NoSQLTable(__classPrivateFieldGet(this, _NoSQL_requester, "f"), { tableDetails: tableData }));
    }
    table(input) {
        if (typeof input === 'string') {
            wrapValidators(() => {
                isValidInputString(input, 'table_id', true);
            }, CatalystNoSQLError);
            return new NoSQLTable(__classPrivateFieldGet(this, _NoSQL_requester, "f"), { tableId: input });
        }
        return new NoSQLTable(__classPrivateFieldGet(this, _NoSQL_requester, "f"), { tableDetails: input });
    }
}
_NoSQL_requester = new WeakMap();
export { NoSQLByte, NoSQLByteSet, NoSQLEnum, NoSQLItem, NoSQLMarshall, NoSQLNumberSet, NoSQLStringSet, NoSQLTable, NoSQLUnMarshall };
